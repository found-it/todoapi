# todoapi

```
docker build -t jpetersenames/todoapi:latest -f .\Dockerfile .
docker run --rm --name todoapi -p 5000:80 jpetersenames/todoapi:latest
docker run --rm --name todoapi -p 9000:9000 jpetersenames/todoapi:latest
```
